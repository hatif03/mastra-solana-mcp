import { createPrompt } from "@mastra/mcp";

export const promptHandlers = {
  // Add any prompt handlers here if needed
  // For now, we'll keep it empty as the original implementation didn't have prompts
};

