import { createResource } from "@mastra/mcp";

export const resourceHandlers = {
  // Add any resource handlers here if needed
  // For now, we'll keep it empty as the original implementation didn't have resources
};

